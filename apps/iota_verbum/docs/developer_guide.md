# Iota Verbum API v1 (Quick Guide)

Auth: send header X-API-Key: YOUR_KEY.

GET /report/latest â†’ latest .docx or .txt
GET /phases/status â†’ JSON phase completion
GET /arcs/canonical â†’ canonical_arcs.json
GET /variants/provenance â†’ provenance.jsonld
GET /iv/pairs â†’ iv_pairs_manifest.json
GET /atlas/index â†’ serves results/atlas/index.html